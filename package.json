{
  "name": "creator-flow",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm --prefix frontend install && npm run build --prefix frontend && mkdir -p dist && cp -r frontend/dist/* dist/"
  }
}
